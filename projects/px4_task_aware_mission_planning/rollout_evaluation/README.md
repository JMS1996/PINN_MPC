# Rollout Evaluation

Evaluation scripts for:

```text
task sequence -> surrogate rollout -> ETA / error / risk / cost
```

The first comparison should be:

```text
nominal ETA vs PINN rollout ETA vs PX4 SITL ETA
```
